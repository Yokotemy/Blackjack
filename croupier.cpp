#include <iostream>
#include<vector>
#include "croupier.h"

//w sumie to ja to wszystko jak na razie w start() robie

/*void croupier::showDeck(int round) {
    if (round == 1) {
        hands[0].getCard(0).displayCard();
        std::cout << " ?" << std::endl;
    }
    else {
        hands[0].displayHand();
    }
}
void croupier::takeCard(deck& D) {
    if (hands[0].isEmpty()) {
        hands[0].takeCard(D.giveCard());
        hands[0].takeCard(D.giveCard());
    }
    else {
        while (hands[0].calculateHand() < 18) {
            hands[0].takeCard(D.giveCard());
        }
    }
}*/